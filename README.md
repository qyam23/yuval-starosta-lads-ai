# AI2 Review Renew

This repository was cloned locally from `qyam23/YUVAL-STAROSTA-ENGINEERING` and is now being reshaped into the new `AI2 Review Renew` website.

The existing project data and structure were kept in place so we can continue from this baseline and build the renewed experience on top of it.

Machine-readable docs: `/llms.txt`

## Original local setup

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/c402ca3f-c8f9-42fc-a57d-ff571d5c925d

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
